{
  "name": "jellyHTML5",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
  },
  "devDependencies": {
    "jshint": "0.9.1",
    "jasmine-node": "1.0.28",
    "istanbul": "0.1.27"
  },
  "scripts": {
    "pretest": "jshint --config jshint/src.json src && jshint --config jshint/test.json test",
    "test": "istanbul test jasmine-node -- test"
  }
}
